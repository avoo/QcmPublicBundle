{% extends 'QcmPublicBundle::layout-welcome.html.twig' %}

{% block content %}
    <h4 class="text-center">{{ 'qcm_public.questionnaire.end.title'|trans }}</h4>

    <div class="panel-body text-center">
        {{ 'qcm_public.questionnaire.end.content'|trans|raw }}

        <div class="span7 text-center">
            {{ actions.cancel(path('qcm_core_security_logout'), 'qcm_public.button.logout'|trans, 'fa-sign-out') }}
        </div>
    </div>
{% endblock %}
